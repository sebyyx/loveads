---
deployment:
  tasks:
    - export DEPLOYPATH=/home/loveads18/public_html/
    - rsync -av --exclude='.git' --exclude='.cpanel.yml' --exclude='config.php' --exclude='docs' --exclude='NOTES.md' --delete . $DEPLOYPATH
